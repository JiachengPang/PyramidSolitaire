/**
 * Represent an interaction between the user input and the output of the program.
 * This is only for testing the controller of the pyramid solitaire game.
 */
public interface Interaction {
  void apply(StringBuilder in, StringBuilder out);
}
